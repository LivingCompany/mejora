<%
	if @task.save
		@task = Task.new
	end
%>
$("#tasks_form").remove();

$('#modal_task_form').modal('hide');
$( "#project" ).fadeOut( "slow", function() {
	$('#project').html("<%= j render 'projects/show_project', project: @project %>").fadeIn();
});
